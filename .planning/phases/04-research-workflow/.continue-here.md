---
phase: 04-research-workflow
task: complete
total_tasks: 4
status: paused
last_updated: 2026-05-26T10:38:59.517Z
---

<current_state>
4-Phase 연구 도구 확장 전체 완료 상태. git 깔끔 (미커밋 없음). 
다음 세션에서 새 기능 추가, 버그 수정, 또는 새 Phase 시작 가능.
</current_state>

<completed_work>

## Phase 1 - 교통 핵심 분석 (완료)
- `src/analysis/counting_line.py` — 가상 감지선 통과 차량 계수 (2D 외적 부호 변화 알고리즘)
- `src/analysis/speed_estimator.py` — 픽셀→km/h 속도 추정 (EMA 스무딩, P85 출력)
- `src/analysis/od_matrix.py` — Origin-Destination 행렬 (pointPolygonTest)

## Phase 2 - 도시 분석 (완료)
- `src/analysis/density_heatmap.py` — 적응형 대역폭 KDE 밀도 열지도
- `src/analysis/zone_analyzer.py` — 존 체류시간·점유율·피크 윈도우 분석

## Phase 3 - 통계 내보내기 (완료)
- `src/visualization/stats_exporter.py` — openpyxl 다중 시트 Excel + Plotly HTML 차트
- `src/prediction/model.py` — MC-Dropout 불확실성 `predict_with_uncertainty()` 추가
- `src/visualization/exporter.py` — CSVExporter에 `pred_std_cx/cy` 컬럼 추가

## Phase 4 - 연구 워크플로우 (완료)
- `src/analysis/calibration.py` — 호모그래피 기반 원근 보정 (픽셀↔실세계 좌표)
- `src/analysis/session.py` — JSON 세션 저장/불러오기
- `src/analysis/congestion.py` — Greenshields 기반 혼잡 감지

## 수정된 핵심 파일
- `src/pipeline.py` — `_build_analyzers()` + `run()` 반환값 dict로 변경
- `src/visualization/renderer.py` — `draw_counting_lines()`, `draw_zones()` 추가
- `app.py` — 전면 UI 재설계 (11개 출력, 아코디언 패널)
- `requirements.txt` — openpyxl, plotly, kaleido 추가
</completed_work>

<remaining_work>
현재 미완료 작업 없음. 4 Phase 모두 완료.

다음 가능한 방향:
1. 새 분석 기능 요청 시 → Phase 5로 확장
2. app.py UI 개선 또는 버그 수정
3. LSTM 모델 성능 개선 (더 많은 사전학습 데이터, 아키텍처 변경)
4. 실시간 스트림(RTSP/웹캠) 지원 추가
</remaining_work>

<decisions_made>

- **Analyzer 패턴 사용**: 모든 분석 모듈은 `update(tracks, frame)` + `finalize()` 인터페이스를 구현하고 `pipeline.py`의 `_build_analyzers()`에 등록
- **Gradio UI**: 연구자 친화적 인터랙티브 UI, 아코디언 패널로 분석 옵션 구성
- **이중 내보내기**: Excel(openpyxl) + HTML 차트(Plotly) — 연구 발표용
- **MC-Dropout 불확실성**: 예측 신뢰도를 CSV에 함께 내보냄
</decisions_made>

<blockers>
없음.
</blockers>

<context>
프로젝트: Object Tracking + LSTM 궤적 예측 앱을 교통공학·도시개발·통계 연구용 툴로 확장.

## 스택
- Python, YOLOv11 (`yolo11n.pt`), LSTM (`models/lstm_pretrained.pt`)
- Gradio (UI), PyTorch (딥러닝), OpenCV (영상처리)
- openpyxl, Plotly, kaleido (내보내기)

## 실행 방법
```bash
pip install -r requirements.txt
python app.py
```

## 아키텍처 패턴
새 분석 모듈 추가 시:
1. `src/analysis/new_module.py` 생성 → `update()` + `finalize()` 구현
2. `src/pipeline.py`의 `_build_analyzers()`에 등록
3. `app.py`에 UI 요소 추가
</context>

<next_action>
Start with: 다음 작업할 기능이나 버그를 사용자에게 확인한 후 시작.
새 기능이라면 → `/gsd:discuss-phase`로 Phase 5 기획
버그 수정이라면 → `/gsd:debug`로 디버깅 시작
</next_action>
